# docker-compose run ttex bash

services:

  tabrepo:
    container_name: tabrepo
    env_file: .env
    image: tabrepo
    build:
        context: ../deps/tabrepo
        dockerfile: ../../docker/Dockerfile_tabrepo
    volumes:
      - ../deps/tabrepo:/tabrepo

  jaix:
    container_name: jaix
    env_file: .env
    image: jaix
    depends_on:
        tabrepo:
            condition: service_completed_successfully
    build:
        context: ..
        dockerfile: docker/Dockerfile
    volumes:
      - ../jaix:/jaix
      - ../tests:/tests
      - ../deps/tabrepo:/tabrepo
    shm_size: '8gb'
